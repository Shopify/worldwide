---
en:
  worldwide:
    IN:
      addresses:
        province:
          label:
            default: State
            optional: State (optional)
          errors:
            blank: Select a state
        zip:
          label:
            default: PIN code
            optional: PIN code (optional)
          errors:
            blank: Enter a PIN code
            invalid_for_country: Enter a valid PIN code for %{country}
            invalid_for_country_and_province: Enter a valid PIN code for %{province},
              %{country}
            too_long: The PIN code value is too long
            contains_emojis: The PIN code cannot contain emojis
            contains_restricted_characters: The PIN code cannot have restricted characters
            contains_html_tags: The PIN code cannot contain HTML tags.
